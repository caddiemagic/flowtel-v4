const target=new URL('/flow-fm/moons/',window.location.origin); target.searchParams.set('focus','busy-work'); for(const [key,value] of new URLSearchParams(window.location.search)) if(key!=='focus') target.searchParams.set(key,value); window.location.replace(target.href);
