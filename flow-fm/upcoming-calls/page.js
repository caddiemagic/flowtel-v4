import { renderTopNav,escapeHtml } from '/flow-fm/ui.js?v=0.10.81';
import { loadUpcomingServiceCalls } from '/shared/acuity-scheduling.js?v=0.10.81';
const nav=document.getElementById('topNav'),list=document.getElementById('callsList'),message=document.getElementById('callsMessage'),gate=document.getElementById('callsGate');nav.innerHTML=renderTopNav('upcoming-calls');
function format(value){const d=new Date(value);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('en-US',{weekday:'long',month:'long',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'}).format(d)}
async function init(){try{message.textContent='Opening your upcoming calls…';const result=await loadUpcomingServiceCalls();const calls=result.calls||[];list.innerHTML=calls.length?calls.map(call=>`<article class="call-card"><header><div><p class="eyebrow">${escapeHtml(call.service_name||'WOMB MAGIC')}</p><h2>${escapeHtml(call.client_name||'Flowtel Guest')}</h2></div><span class="status">${escapeHtml(String(call.status||'scheduled').toUpperCase())}</span></header><div class="call-details"><div><small>Call time</small><strong>${escapeHtml(format(call.starts_at))}</strong></div><div><small>Client timezone</small><strong>${escapeHtml(call.client_timezone||'Flowtel Time')}</strong></div><div><small>Access closes</small><strong>${escapeHtml(format(call.access_until))}</strong></div></div><div class="call-actions"><a href="/cycle-data/?client=${encodeURIComponent(call.client_id)}">Open Client Snapshot</a><a href="/flow-map/?client=${encodeURIComponent(call.client_id)}">Open Flow Map</a></div></article>`).join(''):'<article class="call-card empty"><p class="eyebrow">YOUR CALENDAR IS CLEAR</p><h2>No upcoming calls</h2><p>Calls will appear here after a member books with you.</p></article>';message.textContent='';}catch(e){message.textContent='';gate.hidden=false;gate.innerHTML=`<h2>This room is not open</h2><p>${escapeHtml(e.message)}</p>`;list.innerHTML='';}}
init();
